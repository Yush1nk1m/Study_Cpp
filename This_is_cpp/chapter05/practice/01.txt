1. "a = a"와 같이 자기 자신에게 대입하는 경우
-> l-value와 r-value가 같은 경우에 아무것도 하지 않는 방식으로 해결함

2. "a = b = c"와 같이 대입 연산을 한 줄 안에 연속적으로 수행하는 경우
-> 대입 연산의 반환형이 void일 경우 문제가 생기므로 자기 자신의 참조자를 반환하는 방식으로 해결함

1의 경우, 대입하기 전에 원본 객체가 소멸하는 문제가 발생
2의 경우, void형 반환 값을 대입할 수 없기에 문제가 발생