template<typename T>
T average(const T& first, const T& second)
{
    return (first + second) / 2;
}

template<typename T>
T average(const T& first, const T& second, const T& third)
{
    return (first + second + third) / 3;
}