#include <iostream>
#include <string>

int main() {
    int t;
    std::cin >> t;
    std::cout << "Fail bit turned on. Cannot receive input." << std::endl;
    std::string s;
    std::cin >> s;

    return 0;
}